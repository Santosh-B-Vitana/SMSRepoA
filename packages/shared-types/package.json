{
  "name": "@vitana/shared-types",
  "version": "1.0.0",
  "description": "Shared TypeScript types for Vitana SMS web and mobile apps",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
