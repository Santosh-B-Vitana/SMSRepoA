import { ParentStudentLeaveRequest } from "../components/leave/ParentStudentLeaveRequest";

/**
 * Parent Student Leave Request Page
 * 
 * Allows parents/guardians to:
 * - Request leave for their children
 * - Track leave balance and history
 * - Receive notifications on approval/rejection
 * - Upload supporting documents
 * - View leave request status
 */
export default function StudentLeaveRequestPage() {
  return <ParentStudentLeaveRequest />;
}
