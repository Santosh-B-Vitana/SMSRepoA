
# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
.expo/
# @end expo-cli

# School-specific assets — each school's branding lives outside version control.
# Run `pnpm --filter @vitana/mobile inject:school <schoolId>` to populate.
# Vitana (default) assets ARE committed so the shared app always builds.
assets/school-assets/*/
!assets/school-assets/vitana/

# School config registry — treat as a secret (contains school package names / EAS IDs).
# Managed via secrets manager or private repo for production builds.
# scripts/school-configs.json
